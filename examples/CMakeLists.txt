# dependencies

find_package(Threads REQUIRED)

# third-party

# ...

# flags

llama_add_compile_flags()

# The fork keeps only the Android OSH26 runtime under examples/android_osh26.
# It is a Gradle project, so there is intentionally no CMake target here.
